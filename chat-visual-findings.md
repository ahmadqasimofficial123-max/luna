# Chat Visual Audit

The provided screenshot shows the deployed chat route at `/messages/1` with a selected conversation for Waiz Siddiqui. The active header, participant avatar/name, search, voice-call, video-call, and more controls are visible. The message history renders multiple persisted message bubbles and reaction controls, confirming the selected-conversation route is visually active rather than the empty state. The conversation sidebar is narrow at this viewport, and the history contains many repeated workspace-signal messages; these should be reviewed as data/content behavior, not treated as proof of a hardcoded UI placeholder.

Source: user-provided screenshot attachment `pasted_file_LY1SZd_image.webp`.
